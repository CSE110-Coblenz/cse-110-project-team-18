import { setStageWidth, setDefaultTheme } from "../ui";
import { theme } from "./ThemeConfig";

// Stage dimensions
export const STAGE_WIDTH = 1500;
export const STAGE_HEIGHT = 750;

setStageWidth(STAGE_WIDTH);
setDefaultTheme(theme);